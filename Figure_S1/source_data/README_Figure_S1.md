# Fig S1 (orig S1) — healthy BM reference embeddings

Legacy figure (original-manuscript analysis).

Uses the same scanpy atlas + PCA inputs as `Figure_1` (healthy-BM subset). Rendered UMAPs only; see `../Figure_1/` for the dendrogram code and PCA source data.
