# Figure S1 — Healthy bone-marrow reference

**Self-contained.** Reproduces deployed **Figure S1** (`../../05_Supplementary_Figures/Figure_S1__healthy_BM_reference.pdf`) from its own source data.
Part of the atlas/prognosis analysis stream (legacy Figure_1-4 pipelines); scripts here are copies so this figure stands alone.

## Reproduce
_No in-tree renderer_ (see note below). Plot-level source data is in `source_data/`.

## Note
Rendered by the external scanpy atlas pipeline (healthy-BM subset); UMAP coordinates shipped as source data, raw object to GEO.

## Contents
- `scripts/` — (external; no in-tree renderer)
- `source_data/` — 1 file(s): README_Figure_S1.md
