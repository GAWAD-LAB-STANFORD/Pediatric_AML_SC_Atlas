# Fig S5 (orig S5) — TF-regulon rank plots (per cell type)

Legacy figure (original-manuscript analysis).

Regulon rank plots come from the external **pySCENIC** run (see `../Figure_3/`). Only the top-regulon summary tables (`topreg_*.csv`) and the 312-TF input list are shipped here. GAP: deposit the pySCENIC output.
